"use client";

// import Button from "@/ui/Button";

// import {
//   sendContactEmail,
//   // sendWelcomeEmail,
//   sendResetPasswordEmail,
//   // sendVerifyAccountEmail,
//   sendChangePasswordEmail,
// } from "@/services/emailAPI";

export default function page() {
  // const email = "mohammednassar740@gmail.com";

  // const contact = {
  //   from_name: "Mohamed Nassar",
  //   subject: "Test Hope Contact Email",
  //   from_email: "mohammednassar750@gmail.com",
  //   message: "This is a test contact mail from hope contact form",
  // };

  return (
    <>
      <p>No Data Available Here, this is a test page</p>

      {/* <Button onClick={() => sendWelcomeEmail(email)}>
        Send Welcome Email
      </Button>

      <Button onClick={() => sendVerifyAccountEmail(email)}>
        Send Verify Account Email
      </Button> */}

      {/* <Button onClick={() => sendContactEmail(contact)}>
        Send Contact Email
      </Button>

      <Button onClick={() => sendResetPasswordEmail(email)}>
        Send Reset Password Email
      </Button>

      <Button onClick={() => sendChangePasswordEmail(email)}>
        Send Change Password Email
      </Button> */}
    </>
  );
}
